<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Location is strictly optional: only requested if the user configures Wi-Fi SSID
         filters (Android gates reading the SSID behind it). Coarse must be declared
         alongside fine so the user can choose the approximate-only option. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Camera is only used to scan WireGuard QR codes, and is optional. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Re-confirm the user's identity before exporting a tunnel's private key. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!--
      The WireGuard tunnel AAR (com.wireguard.android:tunnel) already declares
      com.wireguard.android.backend.GoBackend$VpnService with BIND_VPN_SERVICE and
      the android.net.VpnService intent-filter, so we do not redeclare it here.
      The persistent VPN notification is managed by the system.
    -->

    <application
        android:name=".KabelWachtApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:resizeableActivity="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KabelWacht"
        tools:targetApi="29">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KabelWacht">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Invisible trampoline for launcher shortcuts and the widget: toggles a
             tunnel (running the VPN consent flow if needed) and finishes. Exported
             because the launcher fires shortcut intents; the only reachable effect
             is a toast-announced tunnel toggle. -->
        <activity
            android:name=".TunnelActionActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <receiver
            android:name=".widget.TunnelWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_tunnel_info" />
        </receiver>

        <service
            android:name=".vpn.automation.ConditionMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors network changes to connect or disconnect WireGuard tunnels automatically according to user run conditions" />
        </service>

        <receiver
            android:name=".vpn.automation.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
