# The single source of truth for the app version.
# Bump this integer for each release, commit it, then push the tag `v<versionCode>`
# (e.g. version 2 -> `git tag v2 && git push origin v2`).
# Both versionCode and versionName are derived from it.
versionCode=5
